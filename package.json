{
  "name": "locatemvp",
  "private": true,
  "version": "1.7.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo dev --filter=811-center --filter=locate720-backend --filter=l720ops",
    "dev:sim": "turbo dev --filter=811-center",
    "dev:backend": "turbo dev --filter=locate720-backend",
    "dev:web": "turbo dev --filter=l720ops",
    "dev:server": "turbo dev --filter=811-center --filter=locate720-backend",
    "build": "turbo build",
    "lint": "turbo lint",
    "seed": "turbo seed --filter=locate720-backend",
    "reset": "turbo reset --filter=locate720-backend"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "unrs-resolver"
    ],
    "overrides": {
      "lightningcss": "1.30.1"
    },
    "ignoredBuiltDependencies": [
      "bcrypt"
    ]
  }
}
