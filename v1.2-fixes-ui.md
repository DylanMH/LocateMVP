more fixes / ui:

fix date to be mm,dd,yy format for tickets on mobile view
autoscroll feature still buggy - when completing one customer it doesnt autoscroll to the next one. keyboard still covers input boxes and time allocation doesnt float at the top and follow as the page is scrolled through

dev ops:
when viewing a technician profile have the ticket map be in a seperate tab so its not the first thing on the page - it should be a sub tab for tickets in range and ticket map
update the timeshet card to show more details such as what the tech is clocked in under, and better color cordination to differentiate shifts based on the filter selected on main page
add filters for tickets for that tech for open, closed, cleared, marked, rescheduled (we will handle reschudiling later)
any ticket with all customers being cleared will count as a cleared ticket, and any ticket with at least one customer marked will count as a marked ticket
add fiters for ticket types as well, digup, normal, etc
