marking statuses need to keep track of status and then reason - example marked and then paint, paint and flag, etc. same for not marked statuses
track this status in the ticket description on l720 ops and backend
bug?: history of a ticket seems to repeat onsite -> onsite multiple times for some reason, track why and potentially fix

mobile ui:
add haptic feedback to button presses
make time allocation changing easier to understand in timesheet
add a compact list view of tickets as an easier way to navigate all tickets
fix auto scroll when closing customers on customer page, sometimes it doesnt scroll to the close button
have a better input system for time and footage, the keyboard covers a lot of the screen and makes it hard to see what were inputing
remove default 0 when inputing a number. makes it so sometimes user puts in 05 or 02 because they forget to delete the 0
float the time allocation card to the top of screen as its scrolling through customers so its easy to see how much time is left to allocate
user profile stats seem to reset when they clock out even if they clock back in on the same day - the profile should show a 24 hour current day period of tickets closed, only resetting on next day
fix sign out button cutting of the "out" text

mobile additions:
add device location tracking so we managers can see where the tech is located as well as see their route ONLY when they are clocked in
route tracking will only show on individual tech pages BUT tech locations can be visible from map view page for only techs under the supes and managers. meaning, if supervisor bob has bill and ben, he can only see bill and ben, but if the area manager has supervisor bob and tim, he can see all techs under them. District managers can obviously see all techs and supervisors

dev ops:
tech stats dont appear on filter "today" but will appear when switching to other time periods